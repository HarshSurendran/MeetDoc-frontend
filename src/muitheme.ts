import { createTheme } from '@mui/material/styles';

const muitheme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default muitheme;
